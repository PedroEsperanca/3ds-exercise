var models = require('../scripts/models.js');

// jasmine.addFile('lib/tests/Main_spec_node');

console.log(models);

describe("logic works fine", function() {
  it("contains spec with an expectation", function() {
    expect(true).toBe(true);
  });
});